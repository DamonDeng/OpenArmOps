# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/

# Editor
.idea/
.vscode/
*.swp
.DS_Store

# Local overrides (once we add them)
config_local.py
key_bindings.local.json

# Diagnostic snapshots from the Camera Snapshots button
snapshots/

# VR packet recordings copied for offline analysis. Reproducible from
# the live recorder and large enough to bloat the repo, so we don't
# commit them.
vr_recordings_backup/

# Diagnostic / replay artefacts: live VR recordings and motion-worker
# perf-log CSVs. Live next to the code (so config.py's references to
# specific recording filenames are obvious) but the data itself is
# session-specific and reproducible — keep it out of the repo.
local_data/
